var abyss2 = {"entry_two" : [
   {"List": "Realizing the kernel did not support NX stack and only supported certain syscalls."},
   {"List": "Figuring out how to overwrite printf to point at the shellcode using only the known offset to your shellcode (bypass aslr)"},
   {"List": "Writing shellcode to open flag.txt and write it to standard out."}
]};

